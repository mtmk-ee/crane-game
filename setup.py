from setuptools import setup

setup(
    name='crane',
    version='0.0.1',
    description='Crane Game for Kelly',
    author='mtmk',
    install_requires=[
        'pygame',
        'box2d'
    ],
)